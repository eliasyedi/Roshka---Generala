
Se genera una tabla de recurrencia para los numeros de los dados

el metodo generala itera por la tabla para buscar cual se repitio 5 veces

el metodo poker itera por la tabla para buscar cual se repitio 4 veces

full() busca por la tabla aquellos que se repiten 3 y 2 veces simultaneamente;

escalera busca a partir del menor en la tabla si otro por encima se tiene una ocurrencia